@model IEnumerable<BusScedhuleSummery>

<h1>Otobüs Seferleri</h1>

<div class="row">
    <div class="col-12 col-md-8">

        @if (ViewBag.SuccessMessage != null)
        {
            <div class="alert alert-success" id="success_message">
                @ViewBag.SuccessMessage
            </div>
        }

        <div class="text-end mb-2">
            <a asp-controller="BusScedhule" asp-action="Create" class="btn btn-primary btn-sm">Yeni ekle</a>
        </div>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Kalkış</th>
                    <th>Varış</th>
                    <th>Tarih</th>
                    <th>Marka</th>
                    <th>Model</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var busScedhule in Model)
                {
                    <tr>
                        <td>@busScedhule.Id</td>
                        <td>@busScedhule.DepartureName</td>
                        <td>@busScedhule.ArrivalName</td>
                        <td>@busScedhule.Date</td>
                        <td>@busScedhule.VehicleMakeName</td>
                        <td>@busScedhule.VehicleModelName</td>
                        <td>
                            <a asp-controller="Ticket" asp-action="TicketsOfBusScedhule" asp-route-busScedhuleId="@busScedhule.Id">Biletler</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts{

    <script>
        let successMessage = $('#success_message');

        if (successMessage.length) {
            setTimeout(function () {
                successMessage.hide();
            }, 3000);
        }
    </script>

}